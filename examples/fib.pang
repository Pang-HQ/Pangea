import "cstdlib/stdio"

fn fib(n: i32) -> i32 {
    if n <= 1 {
        return n
    }
    return fib(n - 1) + fib(n - 2)
}

fn main() -> i32 {
    let mut n = 0

    while n < 10 {
        printf(c"%d\n", fib(n))
        n++
    }
    return 0
}